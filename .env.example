# 组件名称（可选）
COMPONENT_NAME=wenyan-mcp

# 微信公众号 AppID（必填，用于获取 access_token）
WECHAT_APP_ID=your_appid_here

# 微信公众号 AppSecret（必填，用于获取 access_token）
WECHAT_APP_SECRET=your_appsecret_here

# 主机图片路径前缀（可选，用于本地图片上传时的路径替换）
HOST_IMAGE_PATH=/your/host/image/path

# Docker 容器内图片路径（可选，默认为 /mnt/host-downloads，通常不需要修改）
# DOCKER_IMAGE_PATH=/mnt/host-downloads
